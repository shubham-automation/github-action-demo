apiVersion: v1
kind: Service
metadata:
  name: aks-app-service
  namespace: default
spec:
  selector:
    app: als-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer